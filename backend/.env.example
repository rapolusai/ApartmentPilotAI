# Documentation only: Spring Boot does NOT automatically read this file.
# Enter these names in IntelliJ's Run Configuration -> Environment variables,
# or use the root scripts/Start-Backend.ps1 which prompts for the password.
SPRING_PROFILES_ACTIVE=local
DB_URL=jdbc:postgresql://localhost:5432/partmentpilotai_dev
DB_USER=postgres
DB_PASSWORD=SET_LOCALLY
